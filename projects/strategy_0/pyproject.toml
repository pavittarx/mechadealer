[project]
name = "strategy-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
    "strategylib",
]

[tool.uv.sources]
strategylib = { workspace = true }
